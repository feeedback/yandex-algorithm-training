const fnLogic = () => {};

/**
 * "< task name >" {@link "< contest link >}
 *
 * < description >
 *
 * @param { string[] } lines < input description >
 *
 * @return { number }  < output description & type >
 */
function inputProcessing(lines) {
  // const N = Number(lines[0]);
  // const secondLineNumbers = lines[1].split(' ').map(Number);

  return fnLogic();
}

export default inputProcessing;
